# dist/

Готовая сборка: `pipe_vision/` или `pipe_vision.exe` после `build.bat` / `build.sh`.

~240 МБ — в git не хранится. На новой машине:

```bash
pip install -r requirements.txt
python build_project.py
```

Рядом с exe копируются `config.json`, `NASTROYKI.txt`.
